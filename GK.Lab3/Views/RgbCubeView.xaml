<UserControl x:Class="GK.Lab3.Views.RgbCubeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- LEFT: 3D Cube -->
        <Viewport3D x:Name="Viewport"
                    Grid.Row="0"
                    Grid.Column="0"
                    ClipToBounds="True"
                    MouseDown="Viewport_MouseDown"
                    MouseMove="Viewport_MouseMove"
                    MouseUp="Viewport_MouseUp">

            <!-- Camera -->
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="Camera"
                                   Position="2 2 4"
                                   LookDirection="-2 -2 -4"
                                   UpDirection="0 1 0"
                                   FieldOfView="60"/>
            </Viewport3D.Camera>

            <!-- Light -->
            <ModelVisual3D>
                <ModelVisual3D.Content>
                    <DirectionalLight Color="White"
                                      Direction="-1,-1,-2"/>
                </ModelVisual3D.Content>
            </ModelVisual3D>

            <!-- Cube -->
            <ModelVisual3D x:Name="CubeModel"/>

        </Viewport3D>

        <!-- RIGHT: 2D slice preview -->
        <Border Grid.Row="0"
                Grid.Column="1"
                BorderBrush="Gray"
                BorderThickness="2"
                Width="250"
                Height="250"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Margin="20 0 0 0">
            <Image x:Name="SlicePreview" Stretch="Fill"/>
        </Border>

        <!-- SLIDER -->
        <StackPanel Grid.Row="1"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0 20 0 0">
            <TextBlock Text="Slice (Z): "
                       VerticalAlignment="Center"
                       Margin="0 0 5 0"/>

            <Slider x:Name="SliceSlider"
                    Width="400"
                    Minimum="0"
                    Maximum="1"
                    Value="0.5"
                    ValueChanged="SliceSlider_ValueChanged"/>
        </StackPanel>

    </Grid>
</UserControl>
